WHITESPACE = _{ (" " | NEWLINE) }

Dead = @{ "b" }
Alive = @{ "o" }
Number = @{ ASCII_NONZERO_DIGIT ~ ASCII_DIGIT* }

Cell = ${ Dead | Alive }
Run = ${ Number? ~ Cell }
LineEnd = ${ "$" }
PatternEnd = ${ "!" }

Pattern = { (Run | LineEnd)* ~ PatternEnd }
